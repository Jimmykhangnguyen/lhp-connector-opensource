# Supabase
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your-anon-key

# Cloudinary (used for image thumbnail transforms)
# Base URL is constructed in src/cloudinary.ts — update the hardcoded domain if needed
